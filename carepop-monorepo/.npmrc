# .npmrc

# Prevent pnpm from hoisting React Native related packages to the root node_modules.
# This helps with React Native's native build tooling which expects them locally.
public-hoist-pattern[]=*react-native*
public-hoist-pattern[]=@react-native-community/*
public-hoist-pattern[]=@react-native/*
# Add other patterns here if needed based on build errors
